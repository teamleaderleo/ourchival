{
  "name": "@ourchival/extension",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "node ../../scripts/build-extension-icons.mjs && vite build --mode extension && vite build --mode content && (vite build --mode extension-watch --watch & vite build --mode content-watch --watch & wait)",
    "build": "node ../../scripts/build-extension-icons.mjs && vite build --mode extension && vite build --mode content && node ../../scripts/sync-installed-extension.mjs",
    "lint": "echo \"lint extension\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ourchival/parsers": "workspace:*",
    "@ourchival/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "vite": "^6.0.7",
    "typescript": "^5.7.2"
  }
}
