# Catalog access verification — 2026-09-04

Branch: `codex/compact-agent-export`, based on PR #101 at `4cb2332`.

## Automated checks

- `pnpm exec vitest run convex/catalog.test.ts apps/web/app/catalog-export/catalogExport.test.ts`
  — 7 tests passed: authentication/bounds/field validation; sparse pagination;
  cursor replay/filter binding; import scope/Trash/legacy Inbox behavior;
  Unicode truncation/source preservation; digest/byte identity; empty-page receipts.
- `pnpm test` — 33 Vitest files, 117 tests passed; 3 local-vault Node tests passed.
- `pnpm typecheck` — all workspace packages and Convex passed. The new catalog
  query is explicitly included in Convex's existing selective typecheck config.
- `pnpm --filter @ourchival/web lint` — zero errors; 9 existing hook warnings
  in unchanged components/hooks.
- `pnpm build` — production web and isolated extension builds passed.
  `/catalog-export`: 2.51 kB route, 121 kB first-load JS.
- `git diff --check` — passed.

Test dependencies match the concurrent intake lane: `convex-test` 0.0.54 and
`@edge-runtime/vm` 5.0.0. Existing Convex remains locked at 1.42.1. No production
SDK upgrade is included.

## Browser evidence and limits

Used separate agent-browser sessions `ourchival-catalog-check` and
`ourchival-catalog-final`, isolated Next.js on port 4398, and a synthetic HTTP
fixture on port 4399. No real credentials, archive data, production backend or
existing Edge profile were used. Convex behavior was tested separately with
`convex-test`; this is not a deployed end-to-end verification.

Visually inspected setup, active, success, empty and error states. Checked
390×844, 1280×720 and 1440×900 layouts. Primary controls remain visible and
mobile content has no horizontal overflow. Secondary copy contrast was corrected.

Verified preview → download, source-filter reset, empty-page continuation to
the terminal page, and failure/retry. Stopping the fixture while advancing
preserved the last page and its cursor; restoring it allowed continuation.
The query has a ten-second network timeout. No browser JavaScript errors were
reported during the successful flow.

Downloaded synthetic receipt:

```txt
returned: 2
scanned: 25
NDJSON UTF-8 bytes: 328
hasMore: true
SHA-256: 9898cfd4447d3fc89375617edc03dd35ae197bada6c38cb153c895018d8dc7e1
```

Recomputed the digest and byte count from the downloaded NDJSON using Node's
crypto/Buffer APIs; both matched the manifest. No saved/existing/failed intake
receipt was generated by this read-only slice. Those remain the intake owner's
verification responsibility.

All temporary browser sessions, fixture services and disposable build state
were removed after verification. No deploy, push, PR, runtime reload or storage
destination change was performed. Integrate with the intake branch before an
approved backend/web rollout.
